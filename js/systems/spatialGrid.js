export class SpatialGrid{constructor(size=120){this.size=size;this.cells=new Map()}clear(){this.cells.clear()}key(x,y){return `${Math.floor(x/this.size)},${Math.floor(y/this.size)}`}insert(o){const k=this.key(o.x,o.y);let a=this.cells.get(k);if(!a)this.cells.set(k,a=[]);a.push(o)}query(x,y,r){const out=[],s=this.size,minX=Math.floor((x-r)/s),maxX=Math.floor((x+r)/s),minY=Math.floor((y-r)/s),maxY=Math.floor((y+r)/s);for(let gx=minX;gx<=maxX;gx++)for(let gy=minY;gy<=maxY;gy++){const a=this.cells.get(`${gx},${gy}`);if(a)for(let i=0;i<a.length;i++)out.push(a[i])}return out}}
